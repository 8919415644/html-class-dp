IN html each and every element have their own significance but some elements will not create much 
impact on UI but makes code efficient and SEO friendly.
all semantic elements are block elements 
ex; header,footer,main,address,section,article,etc...

attributes:every elements will have some properties those are called attributes,eg: scr,alt,for 
img element,href,target for a element 

every element can contain attributes like class and id.
 element can contain multiple classes or multiple elements contain same class 
 element must contain only one id and it should be unique


 ai:trainable,m  it works with our data
 ----
 chatgpt
 gemini
